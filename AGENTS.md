# HR Toolkit 项目协作规则

以下为用户于 2026-09-08 明确指定的长期默认规则，适用于本项目后续任务。
验证范围以本文件和用户当次明确授权为准，不沿用上级文件中扩大验证范围的默认建议。

## 验证范围

1. 未经用户当次明确授权，禁止全量构建、打包及发布构建。仅对本次修改涉及的代码进行最小必要的语法与编译检查，不得以“验证可编译性”为由触发全量构建。
2. 禁止主动开展全仓审计、额外静态分析、Lint、类型检查及测试套件。保留理解和修改代码所必需的检查，不得以“更稳妥”为由扩大验证范围、反复审计或进行无关重构。
3. 如果最小编译检查无法独立执行，直接说明限制，不要自动升级为全量构建或其他验证。
4. 只有用户明确要求时，才允许执行上述额外操作；授权仅限当次指定范围，不改变本项目的长期默认规则。
5. 完成任务后，简要说明修改内容及实际执行的验证；未验证的部分如实说明，不得声称已通过。

## GitHub 安装包临时范围

- 当前只构建 `HRToolkit_x.x.x_x64-setup.exe`（Win10/Win11）和 `HRToolkit_x.x.x_win7_x64-setup.exe`（Win7）。
- MSI、macOS DMG、便携 ZIP 暂停构建，相关工作流代码须保留为注释，注明原因和恢复步骤，不得删除或自行恢复。
- 恢复其他包须等待用户明确要求，并同步恢复依赖安装、任务依赖、产物上传及发布元数据名单。
- 更新清单、校验文件和 EXE 所需的内部运行时不属于本次暂停的安装包。
